## BlackRock record: client email set — September 14, 2026 (~23:50 ET, production)

Owner said "continue" on the pending list. Record `2414cc99-5d63-4f5d-9423-f239b5a464a6` already carried date 2026-10-06 and start 18:00 (owner had set them; version 2). Verified first that the admin details edit path saves without queuing notifications (customer email is queued only at intake) and that the change preview reported `changed: [email]`, no operational or schedule impact, no blockers. Then set contact email to ranee.turner@blackrock.com through the owner's signed-in Operations tab (PATCH `/api/admin/inquiries/:id/details`, event version 2 → 3, operations 62 → 63). Note for the owner: the record's menus are The Agave Lover's + The Vibrant Classics while the sent proposal names Neon Shadows + Bold Frequency; left untouched pending the owner's call (Bold Frequency is Reserve-tier under policy v1). Still owner-side: enter and accept the BlackRock proposal when signed (rate 70, machines 2, grace 6, tax 8.875, deposit 50), W-9 for BlackRock AP, Slack webhook (parked).

---

## Owner staff record: no labor cost — September 14, 2026 (~23:45 ET, production)

Owner asked to add the staff record with the no-labor-cost flag. A record already existed (id `8b77dc53-affe-44b3-bd9e-3c12ee83dca3`, name "fareed", role "Barsys team lead", email fareed@barsys.com, rate $0, crew access on, flag off). Updated through the owner's own signed-in Operations tab (same-origin `action:'staff'` command, operations version 61 → 62): `internal: true`, name capitalized to "Fareed", everything else unchanged. Effect: assigned hours never add labor cost; the Confirm-card owner default and the Assign select now use this record. Owner pending list shrinks to: BlackRock record update (ranee.turner@blackrock.com, Oct 6 18:00), BlackRock proposal entry/acceptance when signed, W-9 for BlackRock AP, Slack webhook (parked).

---

## owner-0914 LIVE — September 14, 2026 (~23:30 ET, 100% traffic)

Owner ran the flip. Post-flip checks on the production URL passed: root 200, release `barsys-happyhours-production-owner-0914`, ready true, admin HTML 303, receipts export anon 401, upload anon 403, `/site/events.html` 404, served index has `#quick-remember`, none of the removed blocks, six FAQ entries; served `v3.js` carries the one-step send, `operations.js`/`app.js` carry the owner defaults. Headless Chrome at 1440/390 against production (nothing sent): appMode LIVE, "Send inquiry" on quick step 2 and wizard step 5, 9.0 / 12.2 screens, no console errors. No ERROR-severity logs on the revision. Traffic: `owner-0914` 100%; `delete-0914` (rollback), `landing-0914` (superseded) and `trim-0914` at 0%. **Revision cleanup (~23:40 ET, owner-approved):** tags `landing-0914` and `trim-0914` removed, both revisions deleted. The service now holds `owner-0914` (100%) and `delete-0914` (0%, rollback) only. Production re-checked: release owner-0914, ready true.

---

## Candidate owner-0914 — September 14, 2026 (~23:20 ET, 0% traffic, flip pending; supersedes landing-0914)

Commit `c4867eb` = landing batches 1–3 + owner defaults. Preflight 0; `npm test` 209/209; both browser suites pass. Image `…/barsys-staging/app@sha256:91d2da78e15980fe641c4a0f18f5091cefb43586136c5967d99200a90a9cc624` (Cloud Build `aec48f30`, 32 s). Revision `barsys-happyhours-production-owner-0914` at 0%, tag `owner-0914`, env inherited (`BARSYS_RECEIPT_BUCKET` present). Tag-URL smoke passed: root 200, release string, ready true, admin 303, receipts export anon 401, upload anon 403, served `operations.js`/`app.js` carry the owner defaults, `v3.js` carries the send code, `/site/events.html` 404. Flip (owner): `gcloud run services update-traffic barsys-happyhours-production --project happy-hour-landing-version-2 --region us-east4 --to-revisions barsys-happyhours-production-owner-0914=100`. Rollback `delete-0914`. `landing-0914` (0%) can be deleted after the flip. After the flip: check the production URL (root 200, release `…-owner-0914`, ready) and, on the dashboard, open an event without an owner to see your name prefilled.

---

## Owner defaults to the signed-in user — September 14, 2026 (built; candidate owner-0914 supersedes landing-0914)

Owner: "always add me as default unless I'm not the one signed in." Dashboard only. `admin/operations.js` fetches `/api/auth/me` on load; the Confirm card's Event owner field is prefilled with the signed-in user when the event has no owner (the matching active staff record's name when one exists for that email, otherwise the name derived from the Workspace email, e.g. "Fareed"), and the Assign staff select preselects the signed-in user's staff record when it is not already on the event. `admin/app.js` (event desk) prefills "Assigned to" the same way. Existing owners are never overwritten; nothing is saved until the form is saved. `tests/owner-default.test.mjs` (3 checks); `npm test` 209/209; both browser suites pass; isolated LOCAL_TEST run with a seeded owner session (`verify-owner-default.mjs`): Confirm card and desk default to "Fareed", switch to "Fareed Sedkey" once a staff record with fareed@barsys.com exists, the Assign select preselects that record, an existing owner ("Someone Else") is kept, no console errors. Reminder: the production staff directory needs the owner's record with email fareed@barsys.com and "Barsys team member — no event labor cost" ticked for the staff-name and assign defaults to apply.

---

## Landing page candidate landing-0914 — September 14, 2026 (~23:10 ET, 0% traffic, flip pending)

Batches 1–3 below (one-step inquiry, landing trims, site pages) built as one candidate from commit `c68a5e9`. Preflight 0 world-unreadable files; `npm test` 206/206; both browser suites pass. Image `us-east4-docker.pkg.dev/happy-hour-landing-version-2/barsys-staging/app@sha256:79994a377948f4714dc1585e0b84d8f7b1bc8541bf0c25be88c7e3843acf20cf` (Cloud Build `fe3c298a`, 30 s). Revision `barsys-happyhours-production-landing-0914` at 0%, tag `landing-0914`, env inherited (`BARSYS_RECEIPT_BUCKET` present; startup log `Receipts: gcs`; Slack URL still the placeholder). Tag-URL smoke passed: root 200, release string, ready true, admin 303, receipts export anon 401, upload anon 403, `/site/events.html` 404, contact 200, sitemap without events, served index has `#quick-remember` and none of the removed blocks, six FAQ entries, `v3.js`/`app.js`/`inquiry-client.js` carry the send code. Headless Chrome at 1440/390 against the tag URL (`smoke-candidate.mjs`, nothing sent): appMode LIVE, LIVE one-line runtime notice, no preview vocabulary, quick step 2 and wizard step 5 buttons read **Send inquiry**, contact form five controls, 9.0 / 12.2 screens, no console errors, no ERROR logs on the revision. Flip (owner): `gcloud run services update-traffic barsys-happyhours-production --project happy-hour-landing-version-2 --region us-east4 --to-revisions barsys-happyhours-production-landing-0914=100`. Rollback `delete-0914`. After the flip: check the production URL (root 200, release `…-landing-0914`, ready, "Send inquiry" in served `v3.js`) and, if desired, send one real inquiry from the live page with fareed@barsys.com as the contact to see the receipt card and the inquiry email end to end.

---

## Landing page batch 3: site pages — September 14, 2026 (built, candidate pending)

`scripts/build-site.mjs` regenerated the public content pages (now 35): the visible intro is the one-line tagline while the meta description keeps the longer text, so mixlist and package pages no longer repeat the same sentence twice; `clip()` now ends on a full sentence (Reserve meta was cut at "…and custom"); a mixlist whose detail equals its tagline falls back to the generic line (Silk & Snap). Events stub page removed from the generator, the site nav and the landing footer (`site/events.html` deleted; `pages.json` drives the sitemap). Contact page is one block (email + "include the date, guest count and venue"). Package pages: "Up to N menus. Tax, extra hours and unpriced requests are confirmed in your proposal." Mixlist pages: "Package eligibility is shown in the planner." About copy shortened. Every page closer is now "Plan my event ↗ · An inquiry is not a booking." Landing nav renamed to match the site pages: Experience · Packages · Mixlists · Real events (mobile nav the same plus Plan my event). Verified: `npm test` passes (site-seo sitemap test reads the regenerated `pages.json`); duplicate-sentence scan across all 35 pages: 0.

---

## Landing page batch 2: trims — September 14, 2026 (built, candidate pending)

Same bar as the dashboard: each section keeps only what it needs. index.html: the "What is your team's taste?" studio (image, three tabs, Add this collection) is gone from the Experience section, which now reads heading + the three-line service strip; the films section is retitled "Real events. Real rooms." (nav "Real events" already pointed there) and the six-photo rail follows it directly with only its count and arrows (camera-roll heading, "Another side of the evening" promo card and both foot disclaimers removed; the client-occasion film stays reachable from the film dialog); the three-card "Good times. Clear expectations." section and the closing "MEET YOU AT THE BAR" hero are removed; FAQ cut from ten long answers to six short ones (the policy answers live in the policy dialogs); the mixlist-artwork note and the "covers load normally / remembering selections is optional" box under the drinks carousel are removed; "01 /…05 /" section numbers dropped from the eyebrows; brand-ribbon note shortened; footer carries the single media/scope line and "No online bookings or payments". First-visit privacy popup removed: draft storage stays opt-in through a "Remember my selections on this device" checkbox under the quick planner (`#quick-remember`, wired in `privacy.js` to the same preference record; footer "Choose/Manage your preferences" dialog unchanged). Phone: header CTA hidden ≤700px so one "Plan my event" shows per viewport (hero button or bottom dock; dock label unified). "Staff sign in" moved from the main nav to the footer policies row (`staff-auth.js`). `v3.js` guards the removed taste elements. Page length 12 → 9 desktop screens, 17 → 12 phone screens; closing photo and taste artwork no longer load. Verified: `npm test` 206/206; both browser suites pass; isolated LOCAL_TEST render at 1440/390 (`render-landing.mjs` in scratchpad): sections present, removed blocks absent, FAQ 6, staff link only in footer, header CTA hidden on phone, remember checkbox round-trips the preference, tier changes throw nothing, no console errors. Optional Playwright scripts `tests/curated_media.py` / `readiness_media.py` still reference the removed studio, promo card and popup.

---

## Landing page batch 1: one-step inquiry — September 14, 2026 (built, candidate pending)

Owner review of the landing page against the dashboard bar ("simple and to the point, no fluff"); owner approved batches 1–3 (inquiry path, landing trims, site-page fixes) and parked the Slack webhook. Batch 1 changes the public planner so a connected build sends in one step. Quick planner: step 2 is "Your details" (name, company, email, phone, notes, collapsed review, one marketing checkbox, one line "An inquiry does not hold a date or take payment" with Event terms · Cancellations · Privacy links); its button reads **Send inquiry** (LIVE) / **Save test inquiry** (test modes) / "Create proposal preview" (standalone, no runtime); on submit it POSTs through `window.BarsysInquiry.send()` and shows a receipt card ("Inquiry sent. We'll be in touch at <email> to confirm availability, menus and scope. Nothing is booked or charged." + reference id + Download summary). Full wizard: review step trimmed the same way (contact form drops "Contracting entity" and "Venue or office address"; the BEFORE YOU CONTINUE box, publicity select and four-step timeline are gone), step-5 button sends. `inquiry-client.js` no longer injects "Ready for the Barsys team?" panels; it exposes `BarsysInquiry` (idempotency key per plan signature; unchanged plan re-send returns the same receipt; disabled intake → clear error). Copy: "LOCAL PREVIEW" / "local plan" vocabulary removed from index.html, v3.js, app.js (summary sheet now says EVENT SUMMARY / NOT SENT or INQUIRY SENT / REF …); runtime notice above the planner is one line per mode (test modes keep "Only Save test inquiry stores the inquiry in …"). Publicity preference stays in the state at its default ("discuss") and is no longer asked on the public form — owner may ask for it at proposal time. Backend and API unchanged. Verified: `npm test` 206/206 (new `tests/inquiry-path.test.mjs`, 5 checks); both browser suites pass; isolated LOCAL_TEST run with fresh Chrome at 1440/390 (`verify-inquiry.mjs` in session scratchpad): quick and full paths save, receipt cards render, re-send reuses the receipt, records land in the admin list with the marketing flag. Optional Playwright scripts `tests/readiness_browser.py` / `wizard_smoke.py` still reference the removed entity/publicity fields and need updating before they are used again.

---

## BlackRock proposal sent — September 14, 2026 (evening)

Owner sent `~/Desktop/BlackRock Proposal — October 6 2026.pdf` (7 pages, built on the DataDome HTML template with the real Barsys logo; source `.html` beside it) to Ranee Turner, cc Molly McConnell, with the cover email drafted in this session. Terms: Tuesday October 6, 2026, 6:00–8:00 PM, 60 guests (+6 grace, $70 overage), Signature package with client-supplied bar — 2 Barsys 360s (Neon Shadows, Bold Frequency) + 2 ShakerPro stations, three-person Barsys team, iPads/programming, printed menus and NYC transportation included. $70 × 60 = $4,200 + 8.875% tax = **$4,572.75**; signature holds the date; 50% deposit $2,286.38 due **October 3**; balance $2,286.37 invoiced October 6, due October 13. Offer valid to September 21. Client (BlackRock, via Ranee/Molly) owes: venue by Sept 29, ingredient-list confirmation by Sept 29, final count/allergies by Oct 4, bottles at the bar by 4:30 PM Oct 6. Barsys owes on signing: written date confirmation, deposit invoice, ingredient list (preparation page, 60 guests, both menus). Dashboard record `2414cc99-5d63-4f5d-9423-f239b5a464a6`: owner to set email ranee.turner@blackrock.com, date 2026-10-06, start 18:00 via Edit event details, then enter the proposal (rate 70, machines 2, grace 6, tax 8.875, deposit 50) and record acceptance when signed. Cancellation tiers in the PDF: 0 / 25% ($1,143.19) / 50% ($2,286.38). Reminder: BlackRock AP will likely need a W-9 and vendor setup before Oct 3.

---

## Delete event (soft delete) — September 14, 2026 — LIVE (delete-0914 at 100% ~20:55 ET)

Owner ran the flip. Post-flip checks on the production URL passed: root 200, release `barsys-happyhours-production-delete-0914`, ready true, delete/restore code served on desk, operations and Records, anonymous delete 403, admin HTML 303. Traffic 100% on `delete-0914`; `trim-0914` (rollback), `flow-0914`, `receipts-0914` at 0%. No ERROR-severity logs. Owner deleted the four synthetic inquiries from the desk (~21:00 ET); they remain on Records and health as “Deleted (hidden)”. Revision cleanup (owner-approved): tags removed and revisions `flow-0914` and `receipts-0914` deleted; the service holds `delete-0914` (100%) and `trim-0914` (0%, rollback). Production re-checked: release delete-0914, ready true.

Owner asked to remove the four synthetic inquiries polluting the desk (SYNTHETIC POST-FLIP 0911, TEST ONLY - Email delivery rehearsal, SYNTHETIC OWNER REHEARSAL 0914, SYNTHETIC INTAKE-0911) and for a delete button that removes an event from the UI. The 12-month retention rule blocks removal of young inquiries, so this is a **soft delete**: `POST /api/admin/inquiries/:id/delete` (owner-only, `{version, confirm:true}`) sets `doc.deleted={at,by}` and a history line; `store.list()` excludes deleted records from the desk, work queue, operations and crew lists (SQL JSON filter, PG and SQLite); Records and health lists them as "Deleted (hidden)" with a Restore button (`POST …/restore`). Refused (409) for progressed events — accepted proposal, confirmed booking, operations stage > 0, payments, receipt photos, orders, staffing, live reservations or stock receipts — which keep going through the retention review. UI: "Delete event" in the desk's Manage this event form and in the operations event workspace (only while the event is at Confirm with no accepted proposal), each behind a confirm dialog. Test `tests/event-delete.test.mjs`; `npm test` 201/201; both browser suites pass. **After the flip the owner deletes the four synthetic events from the desk** (ids `6b624116…`, `dd5c0852…`, `ce41a338…`, `fdde0015…`); they stay restorable and retirable on Records and health. Deployed ~20:50 ET: image `sha256:301b85732efb493746c12ed5e9036d493ac318e980b903f7b412ee212f11fc1d`, revision `barsys-happyhours-production-delete-0914` at 0% (inherits `BARSYS_RECEIPT_BUCKET`); tag-URL smoke passed (root 200, release string, ready, delete/restore code served on desk, operations and Records, anonymous delete 403, admin 303). Flip: `gcloud run services update-traffic barsys-happyhours-production --project happy-hour-landing-version-2 --region us-east4 --to-revisions barsys-happyhours-production-delete-0914=100`. Rollback `trim-0914`.

---

## Landing-page inquiries entered — September 14, 2026 (~20:42 ET)

Owner asked for entries from the other landing page's events spreadsheet (Google Sheet `1Fh9dS0IMCnph5qwjZAe54tx3YDUIyZd9wYo31nZannc`, eight rows; the sheet export carries no cell colours, so the "three in yellow" were taken as the two the owner named plus DataDome, which already exists). Created on production through the public intake API (same-origin POST, idempotency keys `landing-sheet-blackrock-2026-09-09-01`, `landing-sheet-nova-2026-09-14-01`):

- **BlackRock** — id `2414cc99-5d63-4f5d-9423-f239b5a464a6`. Executive event, **60 guests** (form said 150), Signature, Neon Shadows (Bold Frequency also requested but Reserve-tier under policy v1 → noted, not selected). Client provides all ingredients, spirits, ice and glassware (glassware: venue). Contact Ranee Turner, ranee.turner@blackrock.com, +44 7500 921571 (in notes/phone). Date, venue undecided.
- **Nova Intelligence** — id `2651388d-a30b-45ee-8fb6-f8c71b4a50e9`. Recruiting event, 30 guests, Classic, menus left for recommendation (requested The Signature Mixlist + Après/Garden Spritz Club are not Classic-tier; "recipe cards" add-on does not exist → notes). Contact Alexa Kayman, alexa@novaintelligence.com, (914) 677-5967. Owner call 09/15 to finalize.

Both records carry `fareed@barsys.com` as the contact email so the automated "Barsys inquiry received" email (worker every 5 min) goes to the owner, not the client; the real emails are in the notes. Replace via Edit event details when ready. Not created: GEM / Danu Venture Group (09/04, 30 guests) — owner confirmed 09/14: do not add.

---

## Trim the fat — September 14, 2026 — LIVE (trim-0914 at 100% ~20:30 ET)

Owner ran the flip. Post-flip checks on the production URL passed: root 200, release `barsys-happyhours-production-trim-0914`, ready true, trimmed `operations.js` / `workflow-model.js` / `operations.css` served, admin HTML 303→login, receipts export anonymous 401. Traffic 100% on `trim-0914`; `flow-0914` (rollback) and `receipts-0914` held at 0%. No ERROR-severity logs.

Owner, after the card-by-card walkthrough: "the process has to be simple and to the point, no fluff." Dashboard-only trim in `admin/operations.js` / `workflow-model.js`; backend rules, data and API unchanged.

- **Each card shows only what its step needs.** Confirm: owner, deadline, agreed revenue (read-only once a proposal is accepted), notes, client payments. Prepare finance card ("Planned costs"): Suggest button, planned column, staffing-reviewed tick, planned totals. Return & close ("Costs, hours & payments"): planned + actual columns, stock values, hours tick, Fill actual paid from receipts, payments. Hidden parts stay in the form as hidden fields so saves are unchanged.
- **Planning card per step.** Prepare: drinks/buffer/machines, menu confirmed, recipes (collapsed), equipment quantities (no Packed ticks yet). Purchase & pack ("Pack the equipment"): only the equipment list with Packed ticks. Run event ("Service plan"): read-only recipes with servings and ingredients plus the packed list — no form the server would reject anyway.
- **No duplicates.** The needs table + recommendation appear once (stock card), not also inside the planning card. Purchase & pack is three cards: Pack the equipment · Buy, receipts & suppliers (Astor quote email folded in as a collapsed block) · Dispatch the load (reservations first, needs/new reservations collapsed). `flowSections[2]` = planning, orders, stock; the separate supplier section is gone.
- **One Continue per step.** Two-stage steps (Purchase & pack = Purchase & arrange + Pack & dispatch; Run event = Arrive & set up + Run & close event) list all four checkpoints under stage headings; Continue advances through every stage whose checkpoints are ticked. Checkpoints for the later stage are tickable in advance (`data-task-ahead`).
- **Copy cut** to one line or removed: cost-table explainer, reservation unit rules, supplier-record guidance, receipt guidance, staffing timezone paragraph, confirm-card sentences, step descriptions. Buttons read Save / Assign / Reserve / Receive. "View cost breakdown" wrapper removed (the table is the card). Phone: the needs table hides price/pack columns (shortage already states packs).
- Verified: `npm test` 200/200; both browser suites pass; walkthrough driver ran the DataDome-like event to Financially closed on the trimmed layout (`qa/walkthrough-0914/`, pre-trim screenshots in `before-trim/`). Deployed ~20:20 ET: image `sha256:58e71c588a764168629b37e524a1ac05a12547dec268136a45394a82a7a001da`, revision `barsys-happyhours-production-trim-0914` at 0% (inherits `BARSYS_RECEIPT_BUCKET`); tag-URL smoke passed (root 200, release string, ready, admin 303, trimmed scripts and CSS served). Flip: `gcloud run services update-traffic barsys-happyhours-production --project happy-hour-landing-version-2 --region us-east4 --to-revisions barsys-happyhours-production-trim-0914=100`. Rollback `flow-0914`.

---

## Card-by-card walkthrough + flow fixes — September 14, 2026 — LIVE (flow-0914 at 100% ~20:10 ET)

Owner ran the flip. Post-flip checks on the production URL passed: root 200, release `barsys-happyhours-production-flow-0914`, ready true, new gap code served in `operations.js` and `workflow-model.js` (css/js no-cache so open tabs pick it up on next load), admin HTML 303→login, receipts export anonymous 401, anonymous upload 403. Traffic 100% on `flow-0914`; no ERROR-severity logs. Rollback `receipts-0914`. **Revision cleanup (~20:20 ET, owner-approved):** removed 22 stale 0% traffic tags and deleted 23 superseded revisions (match-0914 back to mail-retention-0910, plus the untagged 00028-msm); the service now holds only `flow-0914` (100%) and `receipts-0914` (0%, rollback). Production re-checked after cleanup: release flow-0914, ready true. DataDome note: enter quantities for portable chargers, microfiber towels and Clorox wipes on Prepare card 2.1 before reserving equipment; the confirm card now says so.

Owner asked for the whole process, card by card, on a DataDome-like test event (Signature · The Signature Mixlist + The Agave Lover's · 20 guests · Sep 17 · $75 × 20 + $120 transport = $1,763.78). Run on an isolated backend + fresh headless Chrome (not production); 110 screenshots at 1440/390 and the report in `qa/walkthrough-0914/README.md`. Inquiry → desk → preparation → proposal r1 → acceptance → Confirm → Prepare → Purchase & pack (Astor record, Gopuff receipt photo, Agave Syrup received, 17 lines dispatched) → Run event → Return & close (17 loads received, hours, Fill actual paid from receipts, deposit + balance) → Financially closed (profit $1,324.73, margin 81.8%) → Records CSV. No overflow, no console errors.

**Findings:** (F4/F5, bug) at Purchase & pack 3.1 the blank equipment quantities (chargers, towels, wipes) cannot be filled once stock is reserved — equipment-quantity edits are material under `backend/PREPARATION-CONSISTENCY.md` — and the only exit (release all, edit, re-reserve) regressed the event to Plan & budget and reset menu confirmation, Packed flags and checkpoints. The Prepare step recommended reserving first and never flagged the blanks. DataDome is on this exact path. (F1) Confirm step blocked by "Preparation changed" after acceptance without a Still-needed line or jump. (F2) menu-confirm tick silently discarded when saved with a quantity change. (F3) packed-without-quantity error unnamed. (F6/F7 low) released reservations clutter Return & close; Suggest planned costs hides its figures in the collapsed breakdown.

**Fixes (dashboard only; consistency rule unchanged):** `recordGaps` lists `preparationNeedsReview`; Prepare-step gaps get a "Go to the Prepare step →" button from other steps; `prepGaps()` lists blank equipment quantities on the Prepare/Purchase confirm cards with a jump, the equipment list auto-opens with an alert while blanks exist, and the stock card warns before reserving; client-side check names blank-quantity Packed lines; material saves report when the confirmation/Packed ticks were cleared; released reservations collapse; Suggest opens the breakdown. Second walkthrough run: no dead-end, all cards to closeout. `npm test` 200/200 (recordGaps test extended); both browser suites pass. Deployed ~20:00 ET: image `sha256:3660622201e08448a00ee4c148e1396cb7ad55f2560c7e6a28313d7b5305469f`, revision `barsys-happyhours-production-flow-0914` at 0% (inherits `BARSYS_RECEIPT_BUCKET`); tag-URL smoke passed (root 200, release string, ready, admin 303, export anon 401, new gap code served). Flip: `gcloud run services update-traffic barsys-happyhours-production --project happy-hour-landing-version-2 --region us-east4 --to-revisions barsys-happyhours-production-flow-0914=100`. Rollback `receipts-0914`.

---

## Gopuff receipt upload — September 14, 2026 — LIVE (receipts-0914 at 100% ~19:30 ET)

Owner ran the flip. Post-flip checks on the production URL passed: root 200, `/api/status` release `barsys-happyhours-production-receipts-0914`, ready true, receipts export/view anonymous 401, anonymous upload 403 (same-origin rule), admin HTML 303→login with return path, `operations.js` carries the upload form, anonymous inquiry POST without Origin 403. Traffic 100% on `receipts-0914`; no ERROR-severity logs on the revision. Owner confirmed a real receipt upload works in the production UI (~19:40 ET). Rollback `match-0914`. Owner still to do: replace the placeholder `BARSYS_SLACK_WEBHOOK_URL`, tick the no-labor-cost flag on the owner staff record, delete superseded 0% revisions.

Owner photographs a Gopuff (or any supplier) receipt on the phone in the Purchase & pack card, enters amount paid, supplier (defaults to Gopuff when the Gopuff list is non-empty), paid-on date and a note; the file and row are kept per event as financial records and export to CSV for expensing. Built to `deploy/runbooks/receipt-upload-brief.md` with the proxy-GET viewing approach (no signed URLs). Infra done with gcloud: private bucket `gs://happy-hour-landing-version-2-receipts` (us-east4, uniform access, public-access prevention enforced, versioning on), `barsys-production-runtime` granted `roles/storage.objectAdmin` on that bucket only; real put/get/412-no-clobber/remove verified from the Mac, synthetic object purged. Code: `backend/receipt-files.mjs` (GCS JSON API via the SA token; local dir outside production; unconfigured production → 503, never instance disk), `applyOperation` actions `receipt-file` / `receipt-file-remove`, routes `POST/GET /api/admin/inquiries/:id/receipts[/:rid]`, `POST …/:rid/remove`, `GET /api/admin/receipts/export?from&to`; magic-byte type checks (JPEG/PNG/PDF; HEIC → 415 with iPhone guidance; 8 MB → 413); retirement deletes objects with rows and lists `receiptObjects` in the ledger (`disposal-recovery.mjs` validator extended). Dashboard: receipt cards with thumbnails/Open/Remove, client-side downscale to ≤2000 px JPEG, "Fill actual paid from receipts" in the finance card, receipts block also in Return & close, CSV form on Records and health. `npm test` 200/200 (5 new); both browser suites pass, workflow suite uploads a PNG at 390 px (`qa/workflow-receipt-mobile.png`). Deployed 2026-09-14 ~19:20 ET: image `sha256:073bc9dea17c6fc809befb52b564ff5648a50ae42f8f834ce37678b9c9632d2b`, revision `barsys-happyhours-production-receipts-0914` at 0% with `BARSYS_RECEIPT_BUCKET=happy-hour-landing-version-2-receipts`; startup log reads `Receipts: gcs`. Candidate smoke on the tag URL passed every line (root 200, release string, ready, export anon 401, upload anon 403, view anon 401, admin HTML 303, operations.js carries the upload form). Deploy/smoke/flip: `deploy/runbooks/receipts-0914.md`. Flip: `--to-revisions barsys-happyhours-production-receipts-0914=100`. Rollback `match-0914` (receipts additive).

**Found while checking the service:** `BARSYS_SLACK_WEBHOOK_URL` on the service is the literal placeholder `https://hooks.slack.com/services/...`, so the Slack ping passes the URL check and Slack rejects it (502). Replace it with the real webhook (Secret Manager reference preferred).

---

## Purchase list matching — September 14, 2026 — LIVE (match-0914 at 100% ~19:00 ET)

`purchaseNeeds()` ("Buy what is missing" in the supplier/orders view) used exact name+unit matching and listed every ingredient as "stock count needed" even though the shortage table showed them covered. It now accepts a matcher; `admin/operations.js` passes the same name-token + bottle/oz→ml matcher used by the shortage table. Tag `match-0914` (supersedes `gopuff-0914`). Flip: `--to-revisions barsys-happyhours-production-match-0914=100`. Rollback `suggest-0914`.

---

## Purchase & pack: Astor vs Gopuff split + Slack ping — September 14, 2026 (candidate gopuff-0914; flip pending)

Recommendation block now separates spirits (order via the Astor supplier draft) from mixers/syrups/consumables (Gopuff). "Ping Fareed on Slack to order from Gopuff" POSTs the list to `/api/admin/slack-ping` (owner-only, throttled), which forwards to a Slack incoming webhook read from `BARSYS_SLACK_WEBHOOK_URL`; unset → 503 with guidance, and "Copy Gopuff list" works regardless. **Owner setup required:** create an incoming webhook in the Slack workspace (Apps → Incoming Webhooks → channel), then on the service run `gcloud run services update barsys-happyhours-production --project happy-hour-landing-version-2 --region us-east4 --update-env-vars BARSYS_SLACK_WEBHOOK_URL=https://hooks.slack.com/services/…` (prefer a Secret Manager reference for the URL). Tag `gopuff-0914` (supersedes `gaps-0914`). Flip: `--to-revisions barsys-happyhours-production-gopuff-0914=100`. Rollback `suggest-0914`.

---

## Still-needed jump buttons — September 14, 2026 (candidate gaps-0914; flip pending)

Each "Still needed" item on the confirm card now names the specific blank fields (e.g. "Blank: Other") and carries a "Go to part n: <card title> →" button that opens the section card holding the fix. Suggest planned costs now sets Other to $0 when nothing else fills it, since a blank planned figure blocks Continue. `npm test` 195/195; browser suites pass. Tag `gaps-0914` (supersedes `suggest-0914`). Flip: `--to-revisions barsys-happyhours-production-gaps-0914=100`. Rollback `suggest-0914`.

---

## Suggest planned costs — September 14, 2026 — LIVE (suggest-0914 at 100% ~18:40 ET; includes flash/nocache/costs/reco)

Button above the cost table in the finance card fills blank planned fields only: Spirits/mixers = cash purchases from the buy list; Existing office stock sent = `costs.knownUsedCents` (when preparation is complete); Payment fees = 3.49% + $0.49 × 2 on the invoice total (or revenue + tax); Glassware & rentals and Equipment & overhead = $0 (owned). Ice, transport and Other are reported as still needing owner input. Marks the form dirty; owner presses Save event & finances. `npm test` 195/195; browser suites pass. Tag `suggest-0914` (supersedes `reco-0914`). Flip: `--to-revisions barsys-happyhours-production-suggest-0914=100`. Rollback `prefill-0914`.

---

## Event recommendation block — September 14, 2026 (candidate reco-0914; flip pending)

Under the shortage table (stock card and planning card), `shoppingList()` now lists: **Buy or count before the event** — each ingredient that is short or not in counted stock, with ml short, packs × pack size (product name) and estimated cost, plus an estimated purchase total; and **Reserve from office stock** — each covered ingredient with the ml needed and how many units of which inventory item to reserve. Built from `shortageRows()` (shared with the table). `npm test` 195/195; browser suites pass. Tag `reco-0914` (supersedes `costs-0914`; includes flashcards, no-cache, cost table). Flip: `--to-revisions barsys-happyhours-production-reco-0914=100`. Rollback `prefill-0914`.

---

## Shortage table costs + fuzzy stock matching — September 14, 2026 (candidate costs-0914; flip pending)

Owner asked whether ingredient costs exist: yes, `backend/ingredient-prices.json` has sourced pack prices for all 14 DataDome ingredients (Astor Wines, Walmart, WebstaurantStore); only the total ($310.41) was displayed. `shortagesView()` now shows pack · unit price, packs to buy and estimated cost per ingredient from `prep.summary.costs.purchases`, and matches inventory by name tokens with bottle/oz→ml conversion (plain "bottle" = 750 ml; "70z" typo read as 7 oz) when the exact name+unit match fails. Spot-check: all 13 stocked DataDome ingredients match; simple syrup is the only unmatched item. `npm test` 195/195; browser suites pass. Image `sha256:f95bdf9e…`, tag `costs-0914` (supersedes `flash-0914`, includes flashcards + no-cache). Flip: `--to-revisions barsys-happyhours-production-costs-0914=100`. Rollback `prefill-0914`.

---

## Flashcard wizard — September 14, 2026 (candidate flash-0914, includes nocache-0914; flip pending)

Owner: no scrolling; one section card at a time, left to right, auto-continue after each section. Implemented: within a step only the active `.section-card` is shown (`viewSection` state); Previous/Next buttons and dot indicators (plus a square dot for the confirm card) move between cards; a successful save inside a card advances to the next card, then the confirm card; Continue (advance) resets to the next step's first card. Slide-in animation contained by `.wizard{overflow-x:clip}` (an uncontained translate caused a 390px overflow in the workflow suite) and disabled under reduced motion. `npm test` 195/195; lifecycle + workflow browser suites pass. Screenshot `qa/wizard-0914/` not updated (temp shot only). Image `sha256:95aac612…`, tag `flash-0914`; also carries the css/js `no-cache` revalidation from `nocache-0914`. Flip: `--to-revisions barsys-happyhours-production-flash-0914=100`. Rollback `prefill-0914`.

---

## Staffing/stock window prefill — September 14, 2026 — LIVE (prefill-0914 at 100% ~18:05 ET); nocache-0914 candidate pending

Owner: assignment dates should come from the event. `defaultWindow()` in `admin/operations.js` prefills Start/End/Planned hours on the assignment form and Departure/Return on the reservation form from the event date plus accepted load-in/departure (fallback: one hour before service start, one hour after service end). Fields remain editable. `npm test` 195/195; browser suites pass. Image `sha256:61eedf5c…`, tag `prefill-0914`. Flip: `gcloud run services update-traffic barsys-happyhours-production --project happy-hour-landing-version-2 --region us-east4 --to-revisions barsys-happyhours-production-prefill-0914=100`. Rollback `wizard-0914-r2`. Verified on DataDome after a forced script refresh: assignment defaults 2026-09-17 16:00 → 19:30, 3.5 h; reservation window the same. The first check showed blanks because css/js carried `max-age=600`; the owner's tab ran the previous script. Follow-up `nocache-0914` (image `sha256:2e8ddd1f…`) switches css/js to `Cache-Control: no-cache` with Last-Modified 304 revalidation so releases show on the next load; assets keep 24h. Flip: `--to-revisions barsys-happyhours-production-nocache-0914=100`.

---

## Wizard event view — September 14, 2026 — LIVE (wizard-0914-r2 at 100% since ~18:00 ET)

Owner asked for the event workspace to read as a wizard: one card per step rather than a stacked list. Implemented in `admin/operations.js` `focusStage()` and `admin/operations.css`: each stage renders a `.wizard-card` with header (step n of 5, title, requirement text, checkpoint progress), the stage's sections open inside the body, and the existing `#flow-next` footer (Back, checkpoints, Continue). Other sections hidden; "Show all event details" remains. Completed steps reopen for review; upcoming steps stay previewable (the workflow needs Prepare-side menu confirmation before Confirm can advance — hard-locking them broke the isolated workflow suite) and are styled as upcoming. Card header uses a `div`, not `header`, because the global `header{height:90px;display:flex}` rule collapsed it. Screenshots `qa/wizard-0914/wizard-desktop.png` (1440) and `wizard-phone.png` (390), no horizontal overflow. `npm test` 195/195; lifecycle + workflow browser suites pass.

Owner follow-up: make each section its own full card. r2 renders a black step header card (title, description, checkpoint status, progress bar), one numbered full card per section (e.g. 2.1–2.4 in Prepare, always expanded, summary hidden) and a bordered confirm card. Screenshots `qa/wizard-0914/wizard-desktop-confirm.png`, `wizard-desktop-prepare.png`, `wizard-phone-prepare.png`. Candidate `barsys-happyhours-production-wizard-0914-r2` (image `sha256:559699d6…`) at 0%, includes the staff no-labor-cost flag. Flip: `gcloud run services update-traffic barsys-happyhours-production --project happy-hour-landing-version-2 --region us-east4 --to-revisions barsys-happyhours-production-wizard-0914-r2=100`. Rollback `hardening-0914`. Earlier 0% candidates `staff-0914` and `wizard-0914` can be deleted after the flip.

---

## Staff no-labor-cost flag — September 14, 2026 (candidate staff-0914, flip pending)

Owner reported staff assignments "not saving"; the audit log showed no assign attempt reached the server, so the browser's required-field validation was blocking the submit silently (incomplete datetime or empty planned hours inside a collapsed section). Assignment succeeded when all fields were valid. Fix: an `invalid` listener on the operations forms opens the collapsed section and posts "Not saved: complete <field> first" in the status line.

Owner also requires Barsys team members to carry no event labor cost (no bartenders on DataDome). Added `internal` flag on staff records (checkbox on add/edit); assignments of internal staff snapshot `rateCents: 0`; dropdown and headings read "Barsys team · no labor cost". Test added; `npm test` 195/195. Interim on production: owner's staff record set to $0/hour, role "Barsys team lead", assigned to DataDome 16:00–20:00 ET (4 planned hours, rate 0).

Candidate `barsys-happyhours-production-staff-0914` (image `sha256:3c496c46…`) deployed at 0%; smoke passed; flip pending owner: `gcloud run services update-traffic barsys-happyhours-production --project happy-hour-landing-version-2 --region us-east4 --to-revisions barsys-happyhours-production-staff-0914=100`. Rollback `hardening-0914`.

DataDome state after tonight: inquiry status confirmed; proposal Rev 6 accepted ($75/guest × 20 + $120 transport = $1,620; tax 8.875% $143.78; total $1,763.78); deposit $881.89 recorded (via $990.77 entry + $108.88 correction; ledger has no edit/void); revenue $1,620; event at Prepare with "Menu and ingredient quantities reviewed" ticked; "Staff and equipment assigned" left open (equipment not yet in inventory). Backlog: payment void action; booking flags (agreementSigned/dateHeld/paymentReceived) have no UI; proposal send.

---

## Hardening release — September 14, 2026 (evening) — LIVE

**Status:** LIVE. Revision `barsys-happyhours-production-hardening-0914` (source `8862917`, image `sha256:dcb63797…`) serves 100% since ~17:05 ET after the owner ran the traffic flip; rollback `admin-supplies-0914-r3` held at 0%. Post-flip checks on the production URL passed (release string, ready, admin/DataDome 303→login, gzip + caching, no preview copy, og tags, anonymous admin API 401). The static DataDome prep page is removed from the image (restorable from `beeef4d`).

- **Security (P0):** `admin/event-prep-datadome.html` (static page with the DataDome contact's name, email and phone) returned HTTP 200 to anonymous requests on production; the admin HTML gate in `backend/server.mjs` only applied under `STAGING_MODE`. Gate now applies in every mode for `/admin/*.html` except login, and forwards a validated `?next=` so sign-in returns to the requested page (`admin/login.js`).
- **Live copy:** removed "Local preview only. Nothing is sent to Barsys.", "This local build sends nothing.", the "V3.9 / PRIVATE LOCAL PREVIEW · NO BOOKINGS OR PAYMENTS" footer badge, "in this preview" and related wording from `index.html`, `app.js`, `v3.js`, `hero-carousel.js`, `policy-content.js`, `runtime-content.js`. Inquiry button now reads "Send inquiry" to match the runtime notice.
- **Delivery:** gzip for text types, `Cache-Control: public, max-age=600` for css/js and `86400` for `assets/`, `Last-Modified` + 304. HTML stays `no-store`.
- **SEO/social:** `decorate()` adds og:url/type/site_name/locale/title/description/image(+alt) and twitter:card/title/description/image on `/` and all 36 site pages; per-mixlist cover as og:image; Organization schema gains postal address, raster logo and image. Site pages gain favicon; descriptions now 70–160 chars. `scripts/seo-audit.mjs` regexes fixed (the "homepage missing description" finding was a false negative) and it now reports 0 issues (`qa/seo-readiness-0914/audit.md`). Indexing remains OFF.
- **Dashboard:** `categoryOf()` matches on word boundaries (strawberry items were filed under Equipment via "straw", ginger items under Spirits via "gin"); "All items" tab; typed search spans all categories; in-flight guard and preserved scroll on saves; `preparationNeedsReview` surfaced with a link; consistent 401→login with return path across all admin scripts (records.js had none); menu-policy save confirmation; desk "planned guests" excludes cancelled; Records link in operations header; crew page navigation.
- **Verification:** `npm test` 194/194; `python3 tests/run-lifecycle-browser.py` passed both suites; local server smoke on :3199 confirmed headers, tags and redirects.
- **OAuth origins fixed (17:00 ET):** stale `opjs-0914-r2` origin removed; both `run.app` production origins re-added and verified in the console. Five origins total.
- **Observed in production (read-only):** DataDome inquiry `5bf6a32f-7cc9-454a-bf11-8e6752201b02` (Sep 17, 20 guests) is present; four synthetic inquiries (`dd5c0852…`, `6b624116…`, `fdde0015…`, `ce41a338…`) remain and inflate desk KPIs; the retention rule blocks their removal through the UI for 12 months. Staff directory is empty, so no event owner can be assigned and the Confirm step cannot advance. Inventory: 12 counted items saved (v13).

---

## Admin inventory tabs and supplies categorization — September 14, 2026

**Status:** DONE, live. Two code deploys after the morning email activation, both verified in the browser by the owner; recorded here from the 2026-09-14 handoff PDF because this file was not updated at the time.

- r2 `barsys-happyhours-production-admin-tabs-0914-r2` (~14:00 ET): three category tabs (Ingredients / Spirits / Equipment) in the inventory picker on `admin/operations.html`; label wording changes in `admin/inquiry-label.js`, `admin/proposal.js`, `backend/notifications.mjs`; `admin/event-prep-datadome.html` static prep page shipped but not linked from any navigation.
- r3 `barsys-happyhours-production-admin-supplies-0914-r3` (~14:56 ET, 100% traffic): `SUPPLY_KEYWORDS` list added to `categoryOf()` in `admin/operations.js` so consumable supplies (microfiber towels, Clorox wipes, ice bags, disposable cups) display under Equipment instead of Ingredients. Backend `kind:'consumable'` in `backend/api.mjs` deliberately unchanged: switching to `kind:'equipment'` would trigger per-item asset labels, block bulk counts, break pack sizing and drop the items from the shortage view. Display-only fix.
- Image `us-east4-docker.pkg.dev/happy-hour-landing-version-2/barsys-staging/app@sha256:f2798dfc297425d9393eeabaf39ff047df461fcfaec7ee2c0108b7e470dcbc82`, tag `supplies-0914-r3`.
- Rollback ladder (0% traffic, held): `admin-tabs-0914-r2` → `email-0914` (keep through 2026-09-15 15:40 UTC) → `intake-0911`.
- Cleanup done: failed r1 revision auto-collected; five legacy numeric revisions deleted; quarantined home-directory `.git.BROKEN-2026-09-14` removed.
- OAuth JavaScript origins as of 15:39 ET: `http://localhost:3089`, `http://localhost`, `https://happyhours.barsys.com`, and a stale `https://opjs-0914-r2---…run.app` debug origin to remove. The two `run.app` production origins previously registered are missing; sign-in via the Cloud Run default URL (which equals `PUBLIC_ORIGIN`) may be rejected until they are re-added.

**Correction to the handoff PDF:** `https://happyhours.barsys.com` is NOT this Cloud Run service. As of 2026-09-14 its DNS CNAME points to `fareed-bit.github.io` and it serves a separate static GitHub Pages marketing landing (last modified 2026-09-05). `/api/*` and `/site/*` return 404 there. No Cloud Run domain mapping exists; the domain cutover remains an unexecuted draft in `deploy/runbooks/dns-search-console.md`. `barsyshappyhours.com` does not resolve.

**Repository:** the project folder had no Git repository (the PDF's `.git/` reference was inaccurate). A local repository was initialized 2026-09-14 with the r3 tree as the baseline commit. `qa/` stays untracked per `.gitignore`.

**Launch gates:** `reference/launch-gates.json` accessibility gate moved to approved citing the 2026-09-11 owner-run VoiceOver acceptance and mobile ergonomic re-audit. `npm run check:launch` now reports all recorded approval gates complete. `npm test` 194/194 on Node 25.6.1 (the sandbox-only sqlite teardown failures did not reproduce on the Mac).

---

## Customer email activation — September 14, 2026

**Status:** DONE. `BARSYS_BACKGROUND_EMAIL_ENABLED=1`. Public launch surface complete except indexing (deliberate) and custom domain (owner-decision blocker).

- Revision: `barsys-happyhours-production-email-0914` (image `sha256:7faf9c…`, env-only change from `intake-0911`).
- Traffic: 100% on `email-0914`; `intake-0911` preserved at 0% as rollback target for ≥24 hours.
- Rollback: `gcloud run services update-traffic barsys-happyhours-production --region us-east4 --to-revisions barsys-happyhours-production-intake-0911=100`
- Owner-mailbox synthetic id `dd5c0852-336f-4bff-8a62-42dd0d9fd36f` processed post-promotion; both expected inbox subjects delivered to `fareed@barsys.com` (`Barsys inquiry received` + `New Barsys inquiry`, same id in body).
- Full evidence: `qa/email-activation-2026-09-14/README.md`.

**Held state:** indexing OFF (`BARSYS_INDEXING_ENABLED` unset, `noindex,nofollow` still emitted, sitemap.xml empty), no custom domain.

**Synthetic rows now in production PostgreSQL (safe to prune after next-day inspection):**
- `fdde0015-00e3-4b4e-bd16-cb274a6853d3` — Friday candidate-intake rehearsal
- `6b624116-d269-4b97-b8ff-a981f4cd1f86` — Friday post-flip prod smoke
- `dd5c0852-336f-4bff-8a62-42dd0d9fd36f` — today's owner-mailbox rehearsal

**Deferred:**
- DataDome real event (deposit paid) reported missing from admin — owner re-adding later. Recommend attaching deposit receipt to `qa/` on re-add.
- SEO content-surface fixes (7 gaps): og:image on all 37 pages, homepage meta description, LocalBusiness/Menu/OfferCatalog schema, Twitter card. Best before DNS cutover.
- Custom-domain decision (`happyhours.barsys.com` vs `barsyshappyhours.com`) — owner blocker before indexing enablement.

---

## Public intake activation — September 11, 2026
Owner-authorized intake activation deploy executed at the Mac. Preconditions cleared: `gcloud config get-value project` = `happy-hour-landing-version-2` (owner-confirmed), `npm test` 185/185 (4558 ms), `node --test tests/public-live-mode.test.mjs` 1/1 (155 ms), all three prior evidence records on file (`qa/public-intake-2026-09-10.md`, `qa/email-rehearsal-2026-09-10.md`, `qa/mobile-a11y-0911/voiceover-checklist.md`). Deploy pinned to the same image serving `barsys-happyhours-production-site-0911` — `us-east4-docker.pkg.dev/happy-hour-landing-version-2/barsys-staging/app@sha256:7faf9cacdcccf7b510216a6e8c497b21c6af02b8a83dd67cd3a61f76513de3a5` — with only two env changes: `--remove-env-vars STAGING_MODE` and `--update-env-vars BARSYS_BACKGROUND_EMAIL_ENABLED=0`. No code change, no rebuild. New revision `barsys-happyhours-production-intake-0911` deployed `--no-traffic --tag candidate-intake` and verified on the candidate tag URL before promotion: `curl -sI /` returned `HTTP/2 200` (was 303→login), `/api/status.mode` returned `LIVE` with `release` matching the candidate, `/api/ready` returned `{"ready":true}`. Anonymous POST rehearsal on the candidate: no-`Origin` request returned 403 (`same-origin required`) as expected; `Origin` = candidate tag URL returned 403 (`origin not allowed`) confirming `PUBLIC_ORIGIN` binds to the production hostname only; `Origin` = production URL succeeded with `HTTP 201` returning id `fdde0015-00e3-4b4e-bd16-cb274a6853d3`, `mode:INQUIRY_RECEIVED`, `booked:false`; repeat with the same `Idempotency-Key` returned `HTTP 200` and the same id; cross-origin `Origin: https://evil.example` returned 403; anonymous `GET /api/admin/inquiries` returned 401. Traffic promoted with `gcloud run services update-traffic barsys-happyhours-production --region us-east4 --to-revisions barsys-happyhours-production-intake-0911=100`. Post-promotion traffic split: `intake-0911` 100%; `acceptance-0910`, `mail-retention-0910`, `privacy-a11y-0910`, `recovery-0910`, `site-0911` all 0%. Rollback target `barsys-happyhours-production-privacy-a11y-0910` preserved. Post-flip smoke on the production URL `https://barsys-happyhours-production-62880701168.us-east4.run.app`: root returned `HTTP/2 200`, `/api/status` returned `mode:LIVE` with `release:barsys-happyhours-production-intake-0911`, anonymous POST with production `Origin` returned `HTTP 201` id `6b624116-d269-4b97-b8ff-a981f4cd1f86`, repeat with same key returned `HTTP 200` and same id, `/api/ready` returned `{"ready":true}`. Owner completed admin-login inspection at ~11:48 UTC and confirmed both synthetic ids visible in `/api/admin/inquiries` — admin path survived the flip. Held state unchanged: `BARSYS_BACKGROUND_EMAIL_ENABLED=0` so the notification worker queues but does not deliver customer email (`backend/api.mjs:53` gate); `BARSYS_INDEXING_ENABLED` unset so Google indexing remains off, `noindex` still emitted, `robots.txt` still `Disallow: /`; no custom domain, public URL is still the `.run.app` hostname. Two synthetic rows now in production PostgreSQL — `fdde0015-00e3-4b4e-bd16-cb274a6853d3` (candidate rehearsal) and `6b624116-d269-4b97-b8ff-a981f4cd1f86` (post-flip smoke), both marked `SYNTHETIC ... - DO NOT FULFILL` with `qa@example.invalid` — safe to prune after next-day inspection and must not enter a real workflow. Rollback command retained: `gcloud run services update-traffic barsys-happyhours-production --region us-east4 --to-revisions barsys-happyhours-production-privacy-a11y-0910=100`. Next gates: 24-hour observation window closing ~2026-09-12 11:47 UTC (watch `/api/ready`, `/api/status`, worker execution logs, `inquiries` row growth, notification queue depth; any missing scheduler run, 5xx spike, or replication lag alert → rollback); after the window, a separate single-lever deploy for customer email activation using the same `--no-traffic --tag candidate-email` pattern. Evidence at `qa/intake-email-activation-2026-09-11/README.md`.

## SEO readiness audit — September 11, 2026
Ran a source-level SEO audit against every path in `sitePaths` (37 URLs) under production SEO env (`STAGING_MODE` unset, `BARSYS_INDEXING_ENABLED=1`, `PUBLIC_ORIGIN=https://happyhours.barsys.com`). Zero production touches. Method: `scripts/seo-audit.mjs` imports `seoSettings`, `decorate`, `sitemap`, `sitePaths` from `backend/site-seo.mjs` and runs them against source files, extracting title, meta, canonical, og:*, JSON-LD, h1, and img alt signals. **Infrastructure verdict: correct.** Sitemap 37 URLs including homepage, unique title ≤65 chars per page, zero duplicates, canonical URL matches origin per page, exactly one h1 per page, zero images missing alt. **Content-surface verdict: under-instrumented.** Homepage missing description entirely. 31 of 36 site pages have descriptions under 70 chars (Google's soft floor for using an author-written snippet). Zero pages carry `og:image` — the highest-leverage single fix for Slack/LinkedIn share previews on a corporate-events site. All pages emit `Organization` schema but zero emit `LocalBusiness`, `EventVenue`, `Menu`/`MenuSection` (needed on 27 mixlist pages), or `OfferCatalog` (needed on packages/classic/signature/reserve). Twitter card meta absent. None of these block indexing; all block ranking and click-through for the queries a corporate buyer would run. Recommended sequence: content-surface fixes now (source edits, ~3-4h, zero deploy risk), then hostname decision, then intake activation, then DNS cutover, then indexing flag. Evidence at `qa/seo-readiness-0911/README.md` and `audit.md`. Audit script at `scripts/seo-audit.mjs` and is repeatable.

## Runbook correction from source review — September 11, 2026
Owner sent gcloud output showing the Mac's active project was `barsys-coaster`, not the production project `happy-hour-landing-version-2`, and the requested read-only commands failed with permission errors. Also, the sandbox cannot use the Mac's gcloud (write to `~/.config/gcloud/credentials.db` is blocked). In response, read `backend/server.mjs`, `backend/api.mjs`, and `backend/site-seo.mjs` directly and corrected two runbooks against source, not against speculation. Two prior guesses were wrong and are now removed: (1) intake activation and customer email activation do NOT use `BARSYS_PUBLIC_INTAKE_ENABLED` and `BARSYS_EMAIL_CUSTOMER_ENABLED`; those env vars do not exist. The real levers are `STAGING_MODE` (unset to open intake and lift the login redirect) and `BARSYS_BACKGROUND_EMAIL_ENABLED` (set to `1` to enable customer email dispatch, only after the intake step is stable for 24h). (2) The DNS runbook's Part A precondition set now includes a hard `gcloud config get-value project` check pointing at the correct project ID, because the wrong-project misfire happened once today already. The intake activation runbook now includes an exact `gcloud run deploy --no-traffic --tag candidate-intake --remove-env-vars STAGING_MODE --update-env-vars BARSYS_BACKGROUND_EMAIL_ENABLED=0` command block with per-step preconditions and candidate-only verification against `curl -sI` and `/api/status`. Nothing was deployed. Reference: `deploy/runbooks/intake-email-activation.md`, `deploy/runbooks/dns-search-console.md`.

## DNS + Search Console runbook — read-only dry run — September 11, 2026
Ran every non-authenticated verify step in `deploy/runbooks/dns-search-console.md` against the current production Cloud Run origin. Zero writes, no gcloud, no Search Console change. Confirmed today's production state: `/` and `/site/packages.html` return HTTP 303 to `/admin/login.html` (staff sign-in guard on — expected), `X-Robots-Tag: noindex, nofollow` on all responses, `robots.txt` is `Disallow: /`, `sitemap.xml` is empty `<urlset/>`, `/api/status.mode` is `STAGING_TEST`, `/api/ready` is `{"ready":true}`, TLS is the shared Cloud Run `*.a.run.app` Google-issued cert. Two runbook drift items surfaced and fixed in-place: the DNS runbook now lists "anonymous view unlocked" and "`STAGING_MODE=0`" as explicit preconditions before Part A5 (they were implicit), and the intake activation runbook step 5 now verifies the `STAGING_TEST→off` and 303→200 flips on the candidate revision. Evidence at `qa/dns-search-console-dryrun-0911/`. Follow-ups requiring owner auth (`gcloud run services describe`, revisions list, scheduler jobs list) listed for owner-run in the README.

## VoiceOver acceptance — owner-confirmed — September 11, 2026
Owner (Fareed Sedkey) ran the qa/mobile-a11y-0911/voiceover-checklist.md script against production revision `barsys-happyhours-production-site-0911` on their own macOS Safari and iPhone Safari sessions on 2026-09-11 and confirmed pass en bloc — first five checks acknowledged individually, remaining checks aggregated at owner request. Result captured at qa/mobile-a11y-0911/voiceover-result.md as owner-attested; individual per-check narration was not transcribed. Environment field left blank at owner discretion. Blocking issues: none reported. Non-blocking issues: none reported. This lifts the September 10 spoken-audio gate. Screen-reader coverage of new dynamic states introduced by future changes remains a repeat check.

## Activation runbooks drafted — September 11, 2026
Two unapproved runbooks added under `deploy/runbooks/` to structure the two remaining production changes without executing them. `intake-email-activation.md` separates public intake activation from customer email activation into two independent deploys with their own halt and rollback conditions, references the September 10 rehearsal evidence and the live-mode contract test, and lists non-goals to prevent scope creep (no DNS, no indexing, no CRM, no payment). `dns-search-console.md` separates DNS cutover on a to-be-chosen hostname from search indexing enablement (a separate `BARSYS_INDEXING_ENABLED=1` toggle that also requires `STAGING_MODE=0`, non-local, and HTTPS per `backend/site-seo.mjs`); requires the intake+email activation to be stable for at least seven days before indexing is turned on; and calls out the sitemap must list only the 36 site pages plus `/`. Neither runbook has an approval flag. Nothing scheduled. Nothing deployed. No DNS change. No customer message. Search Console not touched.

## Mobile ergonomic CSS fixes applied — September 11, 2026
Owner-approved additive CSS applied to `site/site.css` only (backup `site.css.pre-mobile-ergonomics-bak`). Six additive rules: header nav links, footer links, header logo link and `<summary>` disclosures given `padding-block` + `min-height:44px`; mixlist card CTAs given padding + minimum size; nav and footer links given `min-width:44px` to raise short-word links (`FAQ`, `Privacy`); a mobile-only bump raises `.eyebrow` from 11 px to 13 px. No palette, layout or component change. Re-audit against the sandbox preview: **0 tap targets below 44×44 on all 36 site pages across iPhone SE 375, iPhone 14 390 and iPhone Pro Max 414**; remaining flagged links are inline body-copy references, which WCAG 2.5.5 explicitly exempts. Axe re-scan on the 36 site pages returned **0 violations** at desktop 1440 and mobile 390. `npm test` 185/185 confirmed. Landing page (index.html) intentionally not touched — outside the site-expansion scope. Owner-run VoiceOver acceptance remains open. No source module other than `site/site.css` edited. Nothing deployed.

## Mobile and scripted accessibility audit — September 11, 2026
Local sandbox audit of the 36 new content pages plus landing against a preview server built from the same source as production revision `barsys-happyhours-production-site-0911` (build 1c0732f5-34be-4306-8801-3d78b7f510e0). No deployment, DNS, indexing, intake, email or messages. Baseline `npm test` 185/185 confirmed before and after. 114 mobile records (36 site pages × 3 iOS viewports plus landing at /) and 74 axe runs (37 pages × desktop 1440 and mobile 390) captured. All pages returned 200 with no horizontal overflow. Automated axe (WCAG 2 A/AA + 2.1 + best-practice) reported zero violations on 36 site pages and, once `[data-reveal]` reveal state is emulated, zero on the landing; the pre-reveal `#fafafa` false positives were characterized. Keyboard sweeps: privacy, film and Quick planner dialogs all open, traverse, and Escape to opener cleanly; collection dialog traverses without a trap in this run. Mobile ergonomic gaps recorded but not WCAG A/AA blockers: shared header nav links render ~46×23 px, footer links ~21 px tall, FAQ summary controls ~35 px tall, and the eyebrow line renders at ~11 px — all below the 44×44 target-size and 12 px readability thresholds. Owner-run VoiceOver acceptance still required: script prepared at qa/mobile-a11y-0911/voiceover-checklist.md for macOS Safari and iPhone Safari. No source modules edited. See qa/mobile-a11y-0911/README.md, mobile-review.md and a11y-audit.md.

## Protected website expansion released — September 11, 2026
Production site-0911 serves 100% traffic; rollback privacy-a11y-0910. Build 1c0732f5-34be-4306-8801-3d78b7f510e0. Candidate and post-switch eight deployed checks passed, candidate source bytes matched, protected new-page redirects and empty sitemap verified. Authenticated Chrome loaded the production package page. 185 local tests passed; 36 pages and navigation/assets checked. Staging remains on previous revision. Public intake, customer email and indexing not activated; DNS unchanged. Full spoken assistive and new-page mobile acceptance remain open. See qa/site-expansion-0911/README.md.

## Website expansion candidate — September 11, 2026
Built 36 static content pages from current public config: packages/three package details, mixlist library/27 mixlists, events imagery overview, About, Contact and FAQ. Original landing retained with new footer links and updated title/description. No invented case studies or altered pricing. npm run preview rebuilds pages and standalone. Backend serves canonical/schema and controlled sitemap/robots; BARSYS_INDEXING_ENABLED defaults off, with protected/new HTML authentication preserved. 185 tests pass; all new page asset/navigation links checked; desktop package page inspected in Chrome. Collection Escape restores visibly focused opener after scrolling settles. New-page mobile and actual spoken assistive review remain open. Build 1c0732f5-34be-4306-8801-3d78b7f510e0 succeeded; protected candidate deployment underway. No DNS, public intake, email or indexing activation. See qa/site-expansion-0911/README.md.

## Local collection keyboard hardening — September 10, 2026
Added explicit first/last Tab wrapping to the collection dialog in accessibility.js, retaining native Escape and opener restoration. Connected Chrome on localhost:3097 verified forward and reverse wrap; Escape restored opener identity (immediate snapshot during page scroll did not verify opener visibility). Standalone rebuilt; 183/183 tests passed with network permission. Initial restricted run stalled and was interrupted, not counted as passing. Owner attestation closes alcohol/insurance documentation gates; documents remain outside site/dashboard. Launch checker now has only accessibility pending. Spoken screen-reader testing still unverified. This change is local only; production remains protected on its prior revision. No emails, DNS or public activation.

## Launch evidence follow-through — September 10, 2026
Reviewed existing private insurance source summary. Prepared an unsent focused broker/operations request at ../../outputs/private-operations/launch-evidence-request.md for contracting-entity alignment, liquor-service coverage and DataDome-specific documentation. No evidence of absence inferred from missing paperwork. Collection reverse wrap/Escape passed; forward boundary readings remain inconsistent. Accessibility, alcohol and insurance gates remain pending. No source changes, customer messages or deployment.

## Continued protected browser dialog review — September 10, 2026
Privacy-policy title focus, Tab wrap and Escape opener restoration passed. Film keyboard opening, film switching and Escape restoration passed; labeled muted inline video markup verified, native playback controls not independently tested. Collection last-control boundary again yielded one empty focus read before Close collection; remains inconclusive. Actual spoken assistive-technology acceptance remains open. Documentation only; no deployment or records/messages changed. See qa/privacy-a11y-0910/production-keyboard-review.md.

## Production Chrome keyboard review and owner device result — September 10, 2026
Authenticated Chrome keyboard/DOM review completed for both planner local previews, invalid-field handling, summary/privacy focus and collection selected state/Escape. No inquiry saved or message sent; test tab reloaded afterward. Owner reports iPhone 15 Pro Max visual/touch pass with newest iOS, exact version unverified. Native VoiceOver connection timed out; no spoken screen-reader pass claimed. Accessibility remains pending for full manual/assistive acceptance. Documentation only; no source or deployment changes. See qa/privacy-a11y-0910/production-keyboard-review.md for scoped findings and limitations.

## Privacy operating review and cross-browser remediation — September 10, 2026
Privacy operating review completed with 21 focused tests, full 183-test suite, explicit linked-case/copy procedures and private case template outside app. Independent gs://happy-hour-landing-version-2-privacy-ledgers provisioned with uniform access/public prevention/versioning; anonymous 403, synthetic checksum and isolated restore replay passed. Archive export remains manual; no real records/messages changed. Privacy gate scoped operating review approved.
Isolated Chromium/Firefox/WebKit baseline: 35 passes, three package-dialog harness failures and one real mobile-bar contrast failure. Fixed #mobile-book > span to #595959; corrected harness closes planner between packages. Targeted retest build a34bebae-081b-4540-a030-3ae90acc428b: all 16 checks pass; zero axe violations in recorded scans, incomplete/manual findings remain. Added archive service to privacy notice. Image build 116727b5-8731-4350-b734-c377ca12a19f succeeded. Staging and protected production privacy-a11y-0910 serve 100%; rollback acceptance-0910. Candidate readiness/private-access and source hash checks pass. No public intake/email activation or domain change. Owner subsequently reported iPhone 15 Pro Max visual/touch pass; exact iOS version unverified and VoiceOver not tested. Accessibility gate stays open. See qa/privacy-a11y-0910/review.md and JSON evidence.

## Package filtering activated — September 10, 2026
Owner said “do it” after the acceptance report. Published provisional version 1 through the authenticated owner dashboard on production and staging, keeping Signature Mixlist and Agave Lover’s in Signature to match the existing DataDome scope. Cumulative availability: Classic 4, Signature 9, Reserve 18; 9 review-only collections excluded. Both read-only deployed checkers now pass all 8 checks. Public API snapshots saved under qa/acceptance-cloud-0910/. Only menu policy settings were submitted; the API route writes menu policy only, not event/proposal records. No customer rates, email delivery, public intake mode or domain changed. Production remains protected acceptance-0910.
Chrome initially showed Signature’s 9 choices and both named collections; selecting Classic changed visible button text to 4 Classic collections. Later automation snapshots retained stale accessible labels, so no complete three-tier live-browser pass is claimed. The prior isolated 4/9/18 policy lifecycle test remains valid.
Remaining: service authorization evidence, applicable insurance confirmation, coordinated privacy operating review, and full assistive/physical-device acceptance.

## Frozen release acceptance — September 10, 2026
183 Node tests pass. Isolated Linux Chromium passed customer planners, seven-width layouts, privacy/media controls, staff lifecycle and workflow suites. Published-policy synthetic fixture passed; actual staging/production policy remains unpublished (version 0), so package filtering is NOT active. Owner assignment choice pending; see qa/package-filter-review-2026-09-10.md. Fixed stale status metadata to use Cloud Run K_REVISION. Build bcae8103-e57f-454c-b153-30371911e030 succeeded; staging and protected production acceptance-0910 serve 100%, rollback recovery-0910. Post-switch production readiness/private access/source checks pass; STAGING_MODE=1, background email=0, trusted proxy hops=1 verified. No real records/messages/domain changes. Full assistive/device acceptance, service/coverage evidence and coordinated privacy operating review remain outstanding. See qa/release-acceptance-2026-09-10.md for evidence and failed harness runs.

## Completed-event retirement and restore reconciliation — September 10, 2026
Implemented owner-confirmed eligible seven-year retirement, retaining aggregate financial totals/receipt fingerprints while removing event-linked personal detail and preserving shared stock/staff. Email-linked events remain blocked for coordinated review. Added private ledger download and explicit isolated restore replay with session clearing, delivery quarantine, conflict rollback and pre-closeout refusal. 183 Node tests pass; synthetic Chrome retirement/refresh passed. Final-source Cloud Build a4b5d18e-a75d-4649-9f08-c90304e9577b passed PostgreSQL16 tests and image build. Staging and protected production recovery-0910 serve 100%; rollback mail-retention-0910. Candidate readiness/private-ledger checks and authenticated production Records page passed; protected mode/enqueue-off/trusted-hop settings preserved. No actual records removed; no public activation or DNS change. See qa/recovery-retirement-2026-09-10.md. Full assistive/device acceptance, service/coverage evidence and coordinated privacy operating review remain pending.

## Imported-email disposal — September 10, 2026
Owner-reviewed removal of expired unlinked dashboard Gmail copies implemented, with age/hold/review-version/exact-confirmation checks and same-thread event protection. Email source locks serialize conversion with removal; minimal tombstones prevent reimport while Gmail remains unchanged. All 177 Node tests pass. Isolated PostgreSQL16 race checks passed (build 2a103b6c-fd88-4f28-9e73-260963b2c260); synthetic Chrome removal/refresh passed. Release image build 87d57908-b9bb-4200-8b76-9cecf057266d succeeded. Staging mail-retention-0910 now serves 100%; rollback hardening-0910. Protected production mail-retention-0910 now serves 100%; rollback 00005-w6v. Candidate readiness/private-access checks and authenticated production Records page passed. STAGING_MODE=1, ordinary enqueue=0 and trusted hops=1 preserved. No public activation or real-record removal. See qa/mail-retention-2026-09-10.md. Remaining privacy work: coordinated old-event anonymization and backup-disposal reconciliation; other service/insurance/accessibility gates remain pending.

## Production acceptance follow-through — September 10, 2026
Production revision 00005-w6v remains protected, ordinary email enqueue off, no DNS changes. Actual production wizard/two-email rehearsal passed; error alert delivered and subsequent scheduled health passed. Actual direct Cloud Run ingress verified from two clients; trusted proxy hops=1 and spoof-resistant 30/35 challenge rate-limit test passed. Baseline backup restored successfully to isolated target; twelve tables/config state/write rollback verified, no production writes. Temporary proxy service deleted; restore target deletion operation 23553bc3-72ac-4b40-86b0-a4660000002c completed successfully. Actual COI found and reviewed privately; other-event liquor/service evidence still pending. Four-width Chrome and privacy-dialog keyboard checks passed; no full assistive/device certification. Database base estimate $112.87 production + $29.19 staging/month; HA unchanged pending preference. Correction: menu_policy/mail_settings saved overrides are empty, so earlier copy claim meant no-op/defaults, not seeded rows. Full evidence and remaining gates: qa/production-acceptance-2026-09-10.md.

## Protected production provisioned — September 10, 2026
Production service barsys-happyhours-production-00002-69c is deployed at https://barsys-happyhours-production-62880701168.us-east4.run.app in STAGING_TEST protected mode, customer enqueue OFF. Dedicated regional PostgreSQL16 db-custom-1-3840, backups/PITR/deletion protection verified; restricted barsys_app DB role and Secret Manager URL version1. Production runtime and separate mail identities created. Five-minute production worker/health schedules configured; worker xrtpj and health zjvvz passed. Fareed Google login on production verified in Chrome; existing OAuth origins preserved. Only menu_policy and mail_settings copied; inquiries remain empty. Project monthly $200 alert configured (not spending cap). Production error alert and readiness uptime policy created; production silence policy5042043270607247062 configured (not deliberately triggered). Baseline backup operation f94f120d-b238-44ad-8246-d3170000002c started. No domain/customer activation or real-record removals. Remaining launch checks and source evidence in qa/deployment-execution-2026-09-10.md.

## Deployment execution — September 10, 2026 (in progress)
Staging now serves hardening-0910 at 100%; rollback privacy-0910. 174 local Node tests pass. New owner delivery panel prioritizes failed/unknown jobs. Isolated Cloud Build PostgreSQL16 removal test passed (3da368a7-98c4-43e4-a0ca-4a75d64c8cb0). Connected Chrome synthetic removal confirmation/persistence and four widths passed; authenticated deployed delivery panel verified, no actual records removed. Monitor success metric and absence policy 11824184440116711294 created; normal monitor execution zn2lr passed, silence alert end-to-end not yet tested. Production SQL creation operation 92abcbaf-0a25-40d9-8554-78b60000002c in progress; runtime service account created. No production traffic/domain changes. Proxy trust remains default 0 pending real ingress verification. Privacy/email runbooks added; COI/service docs and human assistive/device checks still outstanding. Build 34a29cc9-3bf5-4429-84ac-eded8dbbeccd succeeded. Read qa/deployment-execution-2026-09-10.md for updated evidence.

## Retention removal follow-up — September 10, 2026
Local-only owner-confirmed removal for expired unconverted wizard inquiries added with transactional hold/link/backlog/version safeguards, receipt tombstones and minimal disposal audit. 173 tests pass. No real records removed; staging stays privacy-0910. Isolated PostgreSQL initdb blocked by sandbox shared-memory denial; native Chrome initially worked then mobile/screenshot/AX failed. Removal UI/PostgreSQL verification pending, so not deployed. COI/service document location requested, not yet supplied. Procedure and remaining Gmail/core-event/backup limitations: backend/RETENTION-OPERATIONS.md and qa/retention-disposal-2026-09-10.md.

## Privacy and production preparation — September 10, 2026
Staging now serves privacy-0910 (100%); rollback retention-0910. Company/address, approved retention review periods/holds, actual Google vendor roles and separate import/sender wording updated. Records-page wrapping/focus/checkbox styling improved. Source-reference banner approval recognized by checker only with neutral disclosure; no false client verification. 170 tests pass and standalone rebuilt. Candidate/post-switch HTTP checks pass. Fresh browser launch and connected Chrome both blocked; no new mobile/screen-reader pass. Production resource plan/release procedure prepared in deploy/, not provisioned. Four launch gates remain, including reviewed service/insurance details, retention removal/privacy completion and assistive technology. No domain or live customer activation. See qa/production-preparation-2026-09-10.md.

## Retention controls and independent monitoring deployed — September 10, 2026
Private staging now serves retention-0910 (100%); rollback email-verified-0910. Owner Records and health page supports dated retention review/holds without deletion, protected by version checks. Whole-queue monitoring and worker heartbeat added; independent five-minute Cloud Run/Scheduler health monitor and external readiness uptime alert configured. Controlled monitor alert reached Fareed Inbox 8:30 PM; normal scheduled execution passed afterward. Chrome hold persistence/clear verified on synthetic record only after renewing expired session. All 169 Node tests passed. No domain/live customer activation. Read qa/retention-monitoring-2026-09-10.md for resource IDs and remaining limitations, including deletion, full retention audit history, new-page accessibility and monitor-scheduler missing-run coverage.

## Retention policy selected — September 10, 2026
Owner delegated a standard retention choice. Recorded a conservative seven-year completed-event core/financial record default with tax-date timing, required-retention/hold exceptions and review before deletion. IRS/NY primary guidance consulted; not described as a universal legal requirement. Existing inquiry/email-copy defaults unchanged. Retention implementation remains pending; no deletion, deployment or privacy gate bypass. See DECISION-REGISTER.md.

## Owner-confirmed company and service facts — September 10, 2026
- Legal entity: Barsys Inc.
- Address: 44 W 37th St, New York, NY 10018.
- Owner confirms Barsys handles alcohol service directly.
- Owner reports holding a COI. No certificate was reviewed or published; coverage, limits and additional-insured details remain unconfirmed and event-specific.
- Completed event/financial retention remains undecided. No licensing or universal service-authorization claim is inferred from the operator confirmation.
Company facts recorded locally in DECISION-REGISTER.md and launch notes; no website or deployment change.

## Production continuation authorized — September 10, 2026
Owner said “do it” in response to final production approval/live inquiry email activation. Authorization recorded; no repeat blanket approval is needed. Read-only launch check still reports five pending operational/accessibility gates and 15 legacy brand-reference flags. No public activation performed: legal identity/address, operator/COI facts, event/financial retention and production/assistive-technology verification remain incomplete. LAUNCH-ORDER.md now supersedes stale pre-email launch notes. Missing owner facts requested; domain remains unchanged.

## Complete staging inquiry email rehearsal passed — September 10, 2026
Quick wizard saved synthetic fe0d479d-1da9-4bb3-8f26-aaa9e13c4e53; event verified in dashboard. Both customer receipt and internal notification delivered to Fareed Inbox at 5:00 PM. PostgreSQL confirms two jobs, accepted, one attempt each. Full suite 165 pass. Staging now serves email-verified-0910, rehearsal marker removed and ordinary customer enqueue disabled; health passes. No public domain changes. Read qa/email-rehearsal-2026-09-10.md for evidence and delivery-status UI limitation.

## Background worker and alerts verified — September 10, 2026
Cloud Scheduler dispatch barsys-notifications-f258s completed successfully. Offline self-send reached Fareed Inbox; deliberate failure execution 6nwm4 generated a verified Google Cloud alert at 4:01 PM. OAuth audience verified Internal. Worker schedule is enabled every five minutes. Public customer inquiry enqueue remains disabled; website service/traffic unchanged. No customer emails or event backfill. See qa/background-email-gcp-2026-09-10.md.

## Background email GCP setup — September 10, 2026
Offline Gmail sender credential stored in Secret Manager after PKCE consent; dedicated OAuth client and Internal audience configured. Cloud Run job barsys-notifications deployed and successful against staging PostgreSQL; every-five-minute Scheduler enabled. Offline self-send receipt verified in Gmail at 4:00 PM. Fareed monitoring channel and ERROR-log alert policy created; deliberate failure test verified in Fareed Gmail Inbox at 4:01 PM. Staging website traffic unchanged; customer enqueue still disabled. See qa/background-email-gcp-2026-09-10.md for resources and limits.

## Background email queue implemented locally — September 10, 2026
Added atomic inquiry/outbox insertion, database job claims, bounded retry on rate-limit rejection and manual-review state for ambiguous sends/crashes. Owner status endpoint and standalone offline-Gmail worker added. Automatic enqueue is opt-in LIVE only; no historical backfill. Full suite: 164 tests pass after correcting SQLite transaction interleaving exposed by concurrent Gmail import regression. Client secret and offline refresh credential are absent locally; browser self-test grant does not provide unattended credentials. Cloud job/scheduler/monitoring not created; no further emails sent. See backend/BACKGROUND-EMAIL.md for activation and remaining PostgreSQL/credential/alert checks.

## Gmail self-test delivered — September 10, 2026
Authorized Google send consent completed in Barsys Chrome profile on local dashboard. App reported Gmail accepted the fixed self-test; Gmail search verified exactly one matching message labeled Inbox at 3:47 PM to the Fareed account, subject Barsys inquiry email delivery test. No customer messages, inquiry imports or deployment. Automatic delivery remains disabled; browser token is not persisted. This proves interactive self-send only, not unattended delivery or monitoring activation.

## Gmail sending preparation — September 10, 2026
Owner approved fareed@barsys.com for inquiry email and operational alerts. Added separate owner-only Gmail self-test endpoint and Email inquiries disclosure requesting send/email identity scopes. Fixed from/to/content; validates token audience, verified email and send scope; no token persistence, automatic retries or automatic customer sending. Three mocked transport tests pass; frontend syntax checked. Not deployed or browser-tested. Google consent and actual self-test remain pending; monitoring recipient approved but monitoring not configured. No messages sent.

## Owner Terminal launch-control verification — September 10, 2026
Latest supplied Terminal run of test:lifecycle:browser passed: 12 lifecycle checks including the new owner email draft preview, plus 12 proposal/operations workflow checks. Both reports have errors: [] and status: passed. Evidence: owner attachment a47e39d5-f8e2-42a3-998a-93bbc1cc1781/pasted-text.txt (latest run at end; preceding errors are historical). Browser execution works in regular Terminal; restricted Codex launch limitation remains. No app fix needed for this run. Email sending, restore drill, owner facts, monitoring activation and VoiceOver/production verification remain open. No deployment or sends.

## Local launch controls — September 10, 2026
Event desk now exposes owner-only inquiry email drafts; delivery stays disabled. Added database-backed /api/ready and an opt-in read-only restored-PostgreSQL verifier. Full suite 158 pass; four strengthened targeted tests pass; standalone rebuilt. Browser runner still fails before Chrome startup; new UI browser regression added but not passed. Actual restore target, provider/sender decision, owner company inputs, monitoring activation and VoiceOver/production checks remain open. No deployment or external sends. See qa/launch-controls-2026-09-10.md.

## Launch follow-up — September 10, 2026
Added local owner-only inquiry notification previews (delivery disabled) and synthetic review wording. All 156 Node tests pass. Read-only GCP confirms staging automated backups and PITR enabled; latest automated backup 1789023600000 successful. Restore drill, actual production checks, VoiceOver acceptance, provider/delivery implementation and owner/company inputs remain open. No deployment, sends, DNS or real-record changes. See qa/launch-followup-2026-09-10.md and qa/inquiry-email-preview.md.

## Terminal readiness and autoplay confirmed — September 10, 2026
Owner supplied latest Terminal output: readiness_browser ALL PASS (nine grouped checks, seven viewport widths, final page-error assertion clear), followed by readiness_media ALL PASS (five grouped autoplay/pause/focus/reduced-motion checks). Combined with earlier wizard ALL PASS, curated media ALL PASS and 23 lifecycle/workflow browser checks, all requested browser suites now have passing Terminal evidence across these runs. The latest opaque-origin planner history fix remains local only, not deployed. Full screen-reader/real-device/production verification and launch decision gates remain separate; do not claim WCAG certification or public-launch approval.

## Readiness preview history fix — September 10, 2026
Latest owner Terminal run passed all readiness interaction and seven-width checks, then failed the uncaught-page-error assertion: closePlanner called history.replaceState in an opaque about:blank origin. Optional URL cleanup now catches SecurityError only; other errors remain visible. Two targeted tests verify focus/background restoration and close notification both with successful and denied history changes. Standalone rebuilt. Local-only application fix; readiness/autoplay rerun remains required; not deployed.

## Third Terminal rerun — September 10, 2026
Curated media suite ALL PASS, including seven widths and no page JavaScript errors/broken requested bundled photos. Readiness quick route passed, then footer click was intercepted by the still-open full-screen planner. Test now explicitly closes planner before footer/privacy interactions and external-section screenshots; hidden/inert controls are not force-clicked. Updated readiness suite and its chained autoplay suite still require rerun. No application/deployment changes.

## Second Terminal browser rerun — September 10, 2026
Corrected photographer and film-description assertions passed. Readiness then stopped on collapsed quick occasion controls; test now opens the existing optional disclosure, matching the passing wizard suite. Media interactions passed through dedicated selection film; screenshot setup then tried clicking intentionally hidden mobile scene tabs. Layout screenshot setup now selects scenes programmatically, as its existing seven-width layout loop already did; it does not claim hidden controls are usable. Syntax validated; Terminal rerun remains required. No application or deployment changes.

## Owner Terminal test results — September 10, 2026
Regular Terminal successfully launched the browser: wizard suite ALL PASS; 11 lifecycle and 12 workflow-browser checks passed, no recorded errors. Readiness stopped at an outdated $4,250 expectation after adding the approved $800 photographer; updated to $5,050. Curated media stopped at an obsolete internal-filename description assertion; now checks configured description and exact configured film source, retaining the hosted-dinner semantic check. These two updated suites require a Terminal rerun. readiness_media did not run because the preceding readiness suite failed. Test-only changes; no deployment.

# Barsys Happy Hours - current project state
Updated September 10, 2026 UTC: lifecycle stabilization released to private staging; public site unchanged.

## Closeout and role-navigation release — September 10, 2026
Financial closeout now requires an exactly settled client balance, using the accepted gross invoice total where available. Partial payments, overpayments and refunds prevent closing until resolved; office return remains a separate completed stage. Existing closed records with unresolved balances display an explicit operations-complete label instead of Financially closed. Workflow gaps direct staff to Client payments. Owner navigation shows Dashboard, crew navigation shows My assignments linking to crew.html, and visitors/unknown roles do not get a dashboard link. Server access rules and private-staging sign-in gate remain unchanged.

151 Node tests passed; 22 focused regressions passed; standalone rebuilt. Isolated Chrome runner exited before its debugging port became available, including after local network permission was granted; no new browser pass claimed. Released to private staging as barsys-happyhours-staging-closeout-0910 (100% traffic), build 36c1fa5e-51b4-4b77-90dd-39f110485c93. Rollback: barsys-happyhours-staging-a11y-0910-followup. Connected Chrome verified the deployed unpaid-balance rejection and successful close after restoring a synthetic payment; persistence and owner Dashboard navigation passed. Only labeled test event 4dba06ba-0db7-4153-a0db-1f686e7485e2 changed. Crew UI remains automated-test verified only. See qa/closeout-navigation-2026-09-10.md.

## Latest private staging release
September 10: accessibility fixes deployed as barsys-happyhours-staging-a11y-0910-followup, serving 100% staging traffic. Release label a11y-0910-followup. Build 4d37fcee-836c-4358-8ea2-20eedd09e63d. Previous actual serving revision barsys-happyhours-staging-00031-fbg is the rollback target, superseding older rollback notes below. Compared its uploaded source: only v3.js, accessibility.css and preview-build.json differed. Candidate health/auth checks passed before traffic switch. Actual Google login as Fareed, dashboard loading and deployed planner focus/contrast verified. No event records edited; no DNS/public site changes. See qa/staging-a11y-0910-followup.md. Earlier local-only notes below describe pre-release history.

## Public intake rehearsal — September 10, 2026
Anonymous connected Chrome local quick inquiry saved with server receipt; keyboard wrap/Escape, global video pause and 320px page/planner reflow passed. Added isolated LIVE-mode API contract test covering anonymous persistence, idempotency, origin rejection and private admin data. This is not production HTTPS/PostgreSQL or screen-reader certification. No staging/domain changes. Details: qa/public-intake-2026-09-10.md.

## Full available regression pass — September 10, 2026
152 Node tests and ten local/staging read-only HTTP checks passed. Connected Chrome custom wizard saved synthetic local receipt 1242f762-f07b-41f2-b891-dc8516653a79 with correct $4,100 subtotal. Python browser suites blocked by missing Playwright; isolated Chrome startup remains blocked. Launch gate remains nonzero. See qa/full-test-2026-09-10.md for coverage and limitations. No deployment or real-record changes.

## Browser test tooling repaired — September 10, 2026
Installed Python Playwright 1.62.0 into ../barsys-test-tools and routed Python npm browser commands through scripts/browser-python.mjs. Pinned dependency in tests/browser-requirements.txt. Wizard retry now imports Playwright and finds installed Chromium, but macOS denies MachPortRendezvousServer registration in the restricted shell. No new browser pass claimed. README includes regular-Terminal commands for that remaining environment restriction. No deployment.

## Current baseline
Active source: this barsys-after-hours-v3-9 folder under Documents/Codex/2026-09-09/files-mentioned-by-the-user-barsys/work.
Vanilla frontend plus Node backend, SQLite locally and PostgreSQL in documented private GCP staging. Preserve the original logo, monochrome design, media/autoplay controls, 27 mixlists, both planners and eight add-ons. Package prices, service baseline and unknown charges remain unchanged.
Catalogue: 129 recipes; 18 collections compatible, two incompatible and seven unverified for 360 service. Package publication and business approvals remain separate.

## Implemented locally
Text-spacing follow-up: at 320px, increased letter/word/paragraph spacing exposed clipped summary statistics. accessibility.css now stacks summary statistics and wraps price rows below 400px. Connected-browser retest confirmed summary scrollWidth equals clientWidth (267px); standalone rebuilt. Local only.
Accessibility follow-up September 10: corrected route-label contrast and full-screen planner keyboard containment, background inertness, dialog semantics and opener restoration. Native collection dialogs still work. All 148 Node tests passed; standalone rebuilt. Connected-browser landing and open-planner axe scans reported no violations (manual contrast checks remain incomplete). Local only, not deployed. Recovery: ../baselines/before-planner-a11y-20260910.
Latest focus pass: the primary “Plan my event” CTA now opens the planner as a dedicated full-screen planning experience rather than competing with the landing page. The mobile hero is visually simplified, secondary hero controls recede, and the floating event dock appears only after planning interaction. The wizard smoke fixture was updated to the approved current add-on pricing/quote model and optional preferences disclosure. Node 148/148 and seven-width wizard smoke pass locally. This focus pass is not yet deployed to private staging.
Previous batch: runtime-specific storage notices; atomic preparation/operations saves with both versions checked; reservation and packing safeguards; documentation consolidation. See qa/consistency-2026-09-09.md.
Current batch: preparation submitter fallback; accepted proposal handoffs and operational event edits invalidate preparation review consistently; readable white text and focus on dark operations action links.
Added an isolated full API lifecycle rehearsal and a repeatable fresh-profile Chrome browser runner. No real records or authentication sessions are used.

## Latest verified results
148 Node tests passed, zero failures/skips, under Node v25.6.1 with serial execution. Eleven targeted browser checks passed using the actual backend and an isolated database; no uncaught JavaScript errors.
API rehearsal covers inquiry through financial closeout, retries, acceptance, prep changes, stock commitments, dispatch, partial returns, payment correction and exactly-once effects. The 390px browser rehearsal now drives the complete operator lifecycle: proposal save, acceptance impact review and handoff, preparation reconfirmation, Plan & Budget, supplier order and receipt, inventory reservation and dispatch, arrival/setup, event run/cleanup, office return with partial consumable return, actual financials, client payment and final financial closeout. It also retains both preparation-interface, stale-save/discard, runtime-notice, hero-autoplay, action-contrast and mobile-layout checks.
Operations widths 320/390/768/1440 and preparation/cookie views at 390px checked. The workflow runner uses a local-only explicit QA rate-limit bypass because a complete synthetic rehearsal intentionally exceeds the normal 30-request/minute abuse-control bucket; staging/live behavior is unchanged. Physical Safari/iPhone, assistive technologies, Google token exchange and deployed PostgreSQL remain unverified in this batch.
Standalone preview rebuilt: 72 embedded assets, no remote mixlist covers. Details: qa/lifecycle-2026-09-10.md; logs/screenshots in qa/.

## Safety and recovery
No public deployment, external messages, payment execution, Gmail import, real-record writes, credential reads or new commercial terms. Private staging was deliberately updated after a 0%-traffic candidate check. Existing local backend processes may still hold older code.
Source rollback: ../baselines/before-lifecycle-20260910-002010. Review-only patch: qa/lifecycle-source.patch; already applied. No project-local Git repository was initialized.
Private staging now serves revision barsys-happyhours-staging-00030-put at 100% traffic, runtime label mobile-20260910-0200; public Barsys DNS/site is unchanged. Mobile polish covers the customer wizard, event desk, proposal flow and operations workspace. Previous rc-0910-0152 remains an immediate rollback target. The release uses DATABASE_URL secret version 2; never directly roll back to a revision using disabled secret version 1. See qa/staging-release-2026-09-10.md.

## Commands and next work
npm test; npm run test:lifecycle; npm run test:lifecycle:browser; npm run preview. The browser command uses installed Mac Chrome in a fresh isolated profile and cleans its own processes.
Owner reports completing the physical iPhone staging review; do not claim the synthetic inquiry persisted unless its server receipt/record is separately confirmed. On 2026-09-10 the owner approved the proposed commercial/menu/contract/privacy/notification defaults recorded in DECISION-REGISTER.md. Approved source pricing now includes $500 additional hour, $5/guest additional mixlist, $8/guest premium garnish when applicable, $350 napkins/stirrers, $12/guest zero-proof station, $15/guest beer & wine, and $800 2-hour photographer; quote-only items remain quote-only. The 2-hour single-event baseline remains. Full Node and browser lifecycle suites pass after the pricing change. Remaining launch decisions are authoritative company/legal/insurance facts, event/financial retention period, and a Barsys-controlled monitoring/escalation channel; then assistive-technology and production-environment verification. Email delivery remains disabled.
Technical readiness pass 2026-09-10: Node, complete lifecycle browser, standalone privacy/readiness, media/motion and seven-width wizard smoke suites pass. The browser workflow runner was hardened against a transient synthetic checklist persistence race; no production behavior was relaxed. See qa/technical-readiness-2026-09-10.md. Deferred owner/company answers are isolated in qa/remaining-owner-inputs.md.
Public release still requires the remaining OPEN decision groups and production verification in LAUNCH-ORDER.md. Do not suppress launch gates, infer missing commercial/legal terms or deploy production without explicit approval.
History: reference/history/project-state-before-consistency-20260909.md.
